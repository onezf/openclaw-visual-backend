services:
  api:
    build: .
    container_name: openclaw-visual-backend
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8787:8787"
    volumes:
      - ${HOME}/.openclaw:/root/.openclaw
