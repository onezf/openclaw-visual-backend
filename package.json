{
  "name": "openclaw-visual-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenClaw visualization backend API (REST wrapper for OpenClaw CLI)",
  "scripts": {
    "setup": "bash ./scripts/bootstrap.sh",
    "dev": "node --watch src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "@fastify/websocket": "^10.0.1"
  }
}
